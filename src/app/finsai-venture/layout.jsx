import { getRouteMetadata } from '../../lib/Metadata';

export const metadata = getRouteMetadata('/finsai-venture');

export default function FinsaiVentureLayout({ children }) {
  return children;
}

