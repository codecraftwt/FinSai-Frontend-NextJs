import { getRouteMetadata } from '../../lib/Metadata';

export const metadata = getRouteMetadata('/rewards-promotion');

export default function RewardsPromotionLayout({ children }) {
  return children;
}

