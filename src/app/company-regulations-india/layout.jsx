import { getRouteMetadata } from '../../lib/Metadata';

export const metadata = getRouteMetadata('/company-regulations-india');

export default function CompanyRegulationsIndiaLayout({ children }) {
  return children;
}

