import { getRouteMetadata } from '../../lib/Metadata';

export const metadata = getRouteMetadata('/rewards-ibprogram');

export default function RewardsIbProgramLayout({ children }) {
  return children;
}

